let utilHandler = require('../handler/UtilHandler');

module.exports = class Message{
    constructor(messageType, messageContent) {
        //TODO Attribute zuweisen (Müssen mit dem Java Objekt übereinstimmen)
        return this;
    } 
};